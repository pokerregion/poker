Installation
============

Simple from PYPI::

    $ pip install poker


Advanced, directly rom package::

    $ git clone git@github.com:pokerregion/poker.git
    $ cd poker
    $ python setup.py install
